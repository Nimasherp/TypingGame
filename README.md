# TypingGame
TypingGame is a small html Game using html/js/css. 
For the words Data I used a .txt file with random words. And I'm using the "fetch" method as a way to get those words, so a server is needed (I used the Live extension on VS).
